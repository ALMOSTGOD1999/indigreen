import { BaseSeeder } from '@adonisjs/lucid/seeders'

export default class ImportUsersSeeder extends BaseSeeder {
  async run() {
    // Placeholder — ready for fresh CSV import when user data is available.
    console.log('ℹ️  No CSV data to import. Skipping user import.')
  }
}
